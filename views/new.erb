<h2>新規登録</h2>
<form method="post" action="/new">
  <textarea placeholder="タイトルを入力" class="title-form" name="title"><%= h(params[:title]) %></textarea>
  <br>
  <textarea placeholder="メモを入力" class="body-form" name="body"><%= h(params[:body]) %></textarea>
  <br>
  <input type="submit" class="button" value="登録">
</form>
